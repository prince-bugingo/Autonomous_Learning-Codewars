function sumMul(n,m){
    let multiples=[];
     for (let index = n; index < m; index++) {
        multiples.push(n);
        n+=n;
     }
    

     for (let i = 0; i < multiples.length; i++) {
        let sum = sum+ multiples[1];
         
        
     }
    }
    
    console.log(sumMul(2,23));
    