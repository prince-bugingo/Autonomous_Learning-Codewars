function findNeedle(haystack) {
     let index;
     let indexOfANeeddle= haystack.findIndex(finding);
     function finding(haystack){
        for (const x of haystack) {
            if (x=== "needle") {
                index = `"found the needle at position 5" ${indexOfANeeddle}`;
            }
        }
     }
  }